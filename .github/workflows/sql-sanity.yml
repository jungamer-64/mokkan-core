name: SQL Sanity Check

on:
  pull_request:
  push:
    branches: [master]

jobs:
  sql-sanity:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run SQL sanity script
        run: bash scripts/check_sql_patterns.sh
